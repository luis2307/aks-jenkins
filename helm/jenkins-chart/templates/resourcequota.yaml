apiVersion: v1
kind: ResourceQuota
metadata:
    name: jenkins-quota
    namespace: { ? { .Release.Namespace } }
spec:
    hard:
        pods: "5"
        requests.cpu: "10"
        requests.memory: "20Gi"
        limits.cpu: "20"
        limits.memory: "40Gi"